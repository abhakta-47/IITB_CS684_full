{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Parse and Build",
            "type": "shell",
            "command": "${workspaceFolder}/parse_build.sh",
            "args": [
                "${selectedText}",
                "${relativeFile}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            },
            "problemMatcher": []
        }
    ]
}